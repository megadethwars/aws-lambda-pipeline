
version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Instalando dependencias (si existen requirements.txt)"
      - 'if [ -f requirements.txt ]; then pip install -r requirements.txt -t .; fi'

  build:
    commands:
      - echo "Listo para desplegar main.py y requirements.txt sin comprimir"

artifacts:
  files:
    - lambda_function.py
    - requirements.txt
